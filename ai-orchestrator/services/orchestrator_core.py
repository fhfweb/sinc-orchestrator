"""Archived legacy module.
The original implementation moved to `legacy/python-provider/orchestrator_core.py`.
The canonical control plane lives under `services/streaming` on port 8765.
"""

ARCHIVED_TO = "legacy/python-provider/orchestrator_core.py"

if __name__ == "__main__":
    raise SystemExit("This legacy module was archived to 'legacy/python-provider/orchestrator_core.py'. Use the canonical FastAPI control plane instead.")
